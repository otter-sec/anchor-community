agents:
  queue: "snapshots"

env:
  gs_bucket: gs://marinade-validator-bonds-mainnet
  SLACK_API: https://slack.com/api/chat.postMessage
  DISCORD_REPORT_FILE: "bid-distribution-discord-report.txt"
  DISCORD_WEBHOOK_VAR: "DISCORD_WEBHOOK_PSR_FEED"
  command_name: 'init-settlement'

steps:
  - input: "What is the epoch to init settlements for?"
    fields:
      - text: "Epoch"
        key: "epoch"
        format: "[0-9]+"
    if: "build.env('EPOCH') == null"
    blocked_state: running

  - input: "What is the claim type to init?"
    fields:
      - select: "Claim type"
        key: "claim_type"
        options:
          - label: "Unified Distribution"
            value: "unified"
          - label: "Institutional Distribution"
            value: "institutional-distribution"
    if: "build.env('CLAIM_TYPE') == null"
    blocked_state: running

  - wait: ~

  - label: ":black_nib: Env variables setup: Init Settlements ${CLAIM_TYPE}/${EPOCH}"
    key: "setup-env-init"
    commands:
    - |
      epoch=${EPOCH:-$(buildkite-agent meta-data get epoch)}
      buildkite-agent meta-data set --redacted-vars='' epoch "$$epoch"
    - |
      claim_type=${CLAIM_TYPE:-$(buildkite-agent meta-data get claim_type)}
      buildkite-agent meta-data set --redacted-vars='' claim_type "$$claim_type"
      config_pubkey=$(./scripts/bonds-config-pubkey.sh "$$claim_type")
      buildkite-agent meta-data set --redacted-vars='' config_pubkey "$$config_pubkey"
    - |
      slack_feed=$(./scripts/bonds-slack-feed.sh "$$claim_type")
      buildkite-agent meta-data set --redacted-vars='' slack_feed "$$slack_feed"
    - |
      printable_rpc_url=$(./scripts/printable-rpc-url.sh "$$RPC_URL")
      echo "Epoch: '$$epoch', Claim Type: '$$claim_type'/'$$config_pubkey', Slack Feed: '$$slack_feed'"
      echo "# RPC_URL='$$printable_rpc_url'"

  - label: ":memo: Annotate retry parameters"
    depends_on: "setup-env-init"
    allow_dependency_failure: true
    commands:
      - claim_type=${CLAIM_TYPE:-$(buildkite-agent meta-data get claim_type)}
      - epoch=${EPOCH:-$(buildkite-agent meta-data get epoch)}
      - slack_feed=$(buildkite-agent meta-data get slack_feed)
      - printable_rpc_url=$(./scripts/printable-rpc-url.sh "$$RPC_URL")
      - |
        cat <<- EOF | buildkite-agent annotate --priority 6 --style info --context "general-info"
        ### Retry environment variables
        If you need to retry the build, use the following parameters:

        \`\`\`
        CLAIM_TYPE=$$claim_type
        EPOCH=$$epoch
        SLACK_FEED=$$slack_feed
        NOTIFY_FEED=${NOTIFY_FEED:-false}
        NOTIFY_DISCORD_FEED=${NOTIFY_DISCORD_FEED:-false}
        PIPELINES_AUTO_APPROVAL=${PIPELINES_AUTO_APPROVAL}
        # DISCORD_WEBHOOK_VAR: webhook env var name (must be set as a secret)
        # RPC_URL='$$printable_rpc_url'
        \`\`\`
        EOF

  - wait: ~

  - label: ":mega: Notification initializing settlements"
    commands:
    - epoch=$(buildkite-agent meta-data get epoch)
    - claim_type=$(buildkite-agent meta-data get claim_type)
    - slack_feed=$(buildkite-agent meta-data get slack_feed)
    - |
      curl $SLACK_API -X POST -H 'Content-Type: application/json; charset=utf-8' \
        -H "Authorization: Bearer $$SLACK_BEARER_TOKEN" -d '{
          "channel": "'"$$slack_feed"'",
          "attachments": [
            {
              "color": "#8000ff",
              "title": "Confirm Settlements Initialization for Validator Bonds ('"$$epoch"')/('"$$claim_type"')",
              "title_link": "'"$$BUILDKITE_BUILD_URL"'",
              "text": "Please review the *'"'"'Unblock'"'"'* if ready to proceed",
              "footer": "<'"$$BUILDKITE_BUILD_URL"'|View in Buildkite>"
            }
          ]
      }'
    if: "build.env('NOTIFY_FEED') == 'true'"

  - wait: ~

  - label: ":hammer_and_wrench: :rust: Build init-settlement"
    commands:
    - '. "$HOME/.cargo/env"'
    - 'cargo build --release --bin init-settlement'
    artifact_paths:
      - target/release/init-settlement

  - label: ":floppy_disk: :arrow_left: :cloud: Downloading merkle trees and settlements"
    key: "download-json"
    commands:
    - epoch=$(buildkite-agent meta-data get epoch)
    - claim_type=$(buildkite-agent meta-data get claim_type)
    - gcloud storage cp "$gs_bucket/$$epoch/$${claim_type}-merkle-trees.json" "./settlement-merkle-trees.json"
    - gcloud storage cp "$gs_bucket/$$epoch/$${claim_type}-settlements.json" "./settlements.json" || true
    artifact_paths:
      - "./settlement-merkle-trees.json"
      - "./settlements.json"

  - label: ":spiral_note_pad: :school_satchel:  Settlement JSON listing"
    commands:
     - buildkite-agent artifact download --include-retried-jobs settlement-merkle-trees.json .
     - claim_type=$(buildkite-agent meta-data get claim_type)
     - bash ./scripts/settlement-json-listing.sh --merkle-trees settlement-merkle-trees.json --claim-type $$claim_type
     - epoch=$(buildkite-agent meta-data get epoch)
     - echo "========================================================"
     - gcloud storage cp "$gs_bucket/$$epoch/$DISCORD_REPORT_FILE" - 2>/dev/null || echo "(discord report not available)"
    depends_on: "download-json"

  - wait: ~

  - label: ":robot_face: Sanity Check Pipeline Uploader"
    commands:
      - |
        claim_type=$(buildkite-agent meta-data get claim_type)
        echo "Claim Type: '$${claim_type}'"
      - buildkite-agent pipeline upload .buildkite/sanity-$${claim_type}.yml

  - wait: ~

  - label: ":closed_lock_with_key: Concurrency gate lock"
    command: echo "--> Start of concurrency gate"
    concurrency_group: 'validator-bonds/init-settlements-${CLAIM_TYPE}'
    concurrency: 1

  - wait: ~

  - label: ":campfire::arrow_right: Create settlements"
    env:
      RPC_URL: "$$RPC_URL"
      RUST_LOG: info,solana_transaction_builder_executor=debug,solana_transaction_builder=debug,builder_executor=debug,solana_transaction_executor=debug,settlement_pipelines=debug,init_settlement=debug
      #  RUST_BACKTRACE: full
    commands:
    - source "$HOME/.cargo/env"
    - epoch=$(buildkite-agent meta-data get epoch)
    - buildkite-agent artifact download --include-retried-jobs target/release/init-settlement .
    - buildkite-agent artifact download --include-retried-jobs settlement-merkle-trees.json .
    - chmod +x target/release/init-settlement
    - source ./scripts/execute-handlers.sh
    - |
      handle_command_execution "$$command_name" \
      ./target/release/init-settlement \
        --input-merkle-tree-collection "./settlement-merkle-trees.json" \
        --operator-authority "$$VALIDATOR_BONDS_OPERATOR_AUTHORITY" \
        --fee-payer "$$PSR_TX_FEE_WALLET" \
        --rent-payer "$$PSR_RENT_PAYER_WALLET" \
        --epoch "$$epoch"
    key: "init-settlement"
    artifact_paths:
      - "./report.*.json"
    retry:
      automatic:
        - exit_status: 100
          limit: 5

  - label: ":memo: Notification setup: Init Settlements"
    key: "notification-setup-init"
    commands:
    - |
      buildkite-agent artifact download --include-retried-jobs "report.$$command_name.*.json" . || \
        echo '{"command":"init-settlement","status":{"success":false,"error_count":1},"summary":"No report found"}' > "./report.$$command_name.0.json"
    - latest_report=$$(ls -v "report.$$command_name."[0-9]*.json 2>/dev/null | tail -n 1)
    - cp "$$latest_report" "./cumulative-report.json"
    - attempts_count=$$(ls -1 "report.$$command_name."[0-9]*.json 2>/dev/null | wc -l)
    - buildkite-agent meta-data set --redacted-vars="" attempts_count "$$attempts_count"
    artifact_paths:
      - "./cumulative-report.json"
    depends_on: "init-settlement"
    allow_dependency_failure: true

  - label: ":printer: Annotate Report"
    key: "annotate-report-init"
    commands:
      - source ./scripts/execute-handlers.sh
      - annotate_execution_report "$$command_name"
    depends_on: "init-settlement"
    allow_dependency_failure: true

  - wait: ~

  - label: ":floppy_disk: :arrow_right: :cloud: Upload artifacts Init Settlements"
    commands:
    - epoch=$(buildkite-agent meta-data get epoch)
    - claim_type=$(buildkite-agent meta-data get claim_type)
    - buildkite-agent artifact download cumulative-report.json . || echo '"UNKNOWN ERROR"' > ./cumulative-report.json
    - gcloud storage cp ./cumulative-report.json "$gs_bucket/$$epoch/buildkite/$${claim_type}-$${command_name}-report.$(date +%s).json"
    depends_on: "notification-setup-init"
    allow_dependency_failure: true

  - label: ":mega: Notification :fast_forward: Monitoring Initialize Settlements"
    commands:
    - source ./scripts/execute-handlers.sh
    - check_command_execution_status "$$command_name" || true
    - epoch=$(buildkite-agent meta-data get epoch)
    - slack_feed=$(buildkite-agent meta-data get slack_feed)
    - attempts_count=$(buildkite-agent meta-data get attempts_count || echo "UNKNOWN")
    - claim_type=$(buildkite-agent meta-data get claim_type)
    - config_pubkey=$(buildkite-agent meta-data get config_pubkey)
    - |
      curl $SLACK_API -X POST -H 'Content-Type: application/json; charset=utf-8' \
        -H "Authorization: Bearer $$SLACK_BEARER_TOKEN" -d '{
        "channel": "'"$$slack_feed"'",
        "attachments": [
          {
            "color": "'"$$notification_color"'",
            "title": "Init Settlements ('"$$claim_type"'): '"$$notification_result"'",
            "title_link": "'"$${BUILDKITE_BUILD_URL}/#$${BUILDKITE_JOB_ID}"'",
            "text": "Claim type: '"$$claim_type"'/'"$$config_pubkey"', init Settlements ('"$$epoch"') after '"$$attempts_count"' attempts",
            "footer": "<'"$${BUILDKITE_BUILD_URL}/#$${BUILDKITE_JOB_ID}"'|View in Buildkite>"
          }
        ]
      }'
    depends_on: "notification-setup-init"
    allow_dependency_failure: true
    if: "build.env('NOTIFY_FEED') == 'true'"

  - label: ":mega: Notification :fast_forward: Discord feed"
    commands:
    - claim_type=$(buildkite-agent meta-data get claim_type)
    - epoch=$(buildkite-agent meta-data get epoch)
    - |
      if [[ "$$claim_type" != "unified" ]]; then
        echo "Discord reporting is only enabled for unified, skipping for '$$claim_type'"
        exit 0
      fi
    - |
      split_command=$(which split > /dev/null 2>&1 && echo "split" || echo "gsplit")
      webhook_url="$${!DISCORD_WEBHOOK_VAR}"
      if [[ -z "$$webhook_url" ]]; then
        echo "WARNING: Webhook env var '$$DISCORD_WEBHOOK_VAR' is not set, skipping"
        exit 0
      fi
      echo "--- Processing discord report '$$DISCORD_REPORT_FILE' -> webhook '$$DISCORD_WEBHOOK_VAR'"
      gcloud storage cp "$gs_bucket/$$epoch/$$DISCORD_REPORT_FILE" "./discord-report.txt"
      # discord max message length is 2000 characters
      $$split_command -C 2000 "./discord-report.txt" './discord-report.txt.chunk-'
      for report_chunk in './discord-report.txt.chunk-'*; do
        report=$(cat "$$report_chunk")
        curl "$$webhook_url" -H "Content-Type: application/json" -d "$(jq -n --arg report "$$report" '{"content": ("```\n"+$$report+"```"), "embeds": null, "attachments": []}')"
      done
      rm -f ./discord-report.txt ./discord-report.txt.chunk-*
    depends_on: "notification-setup-init"
    allow_dependency_failure: true
    if: "build.env('NOTIFY_DISCORD_FEED') == 'true'"

  - wait: ~
    continue_on_failure: true

  - label: ":gear: Upload and get running fund-settlements pipeline"
    commands:
      # NOTE: there is no way easily to define env variable in upload step
      #       if needed in future, it is needed to generate dynamically new pipeline or use trigger
      - "echo Claim Type: '${CLAIM_TYPE}'"
      - buildkite-agent pipeline upload .buildkite/fund-settlements.yml
    depends_on: "init-settlement"
    allow_dependency_failure: true

  - wait: ~

  - label: ":unlock: Concurrency gate unlock"
    command: echo "End of concurrency gate <--"
    concurrency_group: 'validator-bonds/init-settlements-${CLAIM_TYPE}'
    concurrency: 1
