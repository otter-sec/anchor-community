# settlement-common

Shared library for settlement distribution pipelines. Provides common data structures and utilities used by `bid-distribution`, `institutional-distribution`, and `merkle-generator`.
