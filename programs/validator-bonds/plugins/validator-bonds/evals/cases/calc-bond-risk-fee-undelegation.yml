question: >
  For an underfunded validator the auction computed value = 800 SOL, with
  onchainDistributedPmpe = 150 and auctionEffectiveBidPmpe = 50. The config sets
  bondRiskFeeMult = 2.

  Compute, with bondRiskFeeMult = 2: (a) bondRiskFeeSol, (b) paidUndelegationSol,
  (c) explain why paidUndelegationSol does NOT grow past `value` even though the
  fee does. Show each formula step.
facts:
  - '320 SOL'
  - 'Math.min(1'
wrong_facts:
  - '1600 SOL'
  - 'fee split'
